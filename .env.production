# Production build = the bundled static demo (Vercel). No backend required:
# the brand grid, scan replay, and PDF links all resolve against src/data/*
# and public/reports/*. Refresh fixtures with `py scripts/export_static.py`.
NEXT_PUBLIC_STATIC_MODE=1
